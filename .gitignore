node_modules
.next
out
.env.local
.source
next-env.d.ts
*.tsbuildinfo
.DS_Store

# specs/, public/_static/ and content/notebooks/ ARE committed. They are content, not
# build output: this repo is self-contained and nothing is imported or generated from
# another checkout, so `pnpm install && pnpm dev` on a fresh clone is the whole setup.

# Python bytecode from scripts/rst2mdx.py
__pycache__/
*.pyc

# Pinned env for the Python API generator (scripts/pyapi/requirements.txt)
.pixi/
.venv-pyapi/
