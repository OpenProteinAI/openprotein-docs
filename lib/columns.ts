/** The header's shell — footer and any other site-wide chrome must line up with it. */
export const SHELL_COLUMN = 'mx-auto w-full max-w-(--fd-layout-width) px-4';

/** One centred column for every landing section, so the page reads as one composition. */
export const LANDING_COLUMN = 'mx-auto w-full max-w-6xl px-4';
